{
  "name": "pr-welcome-greeter",
  "version": "1.0.0",
  "description": "GitHub Action to welcome new pull requests",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist"
  },
  "keywords": ["github", "action", "pull-request", "welcome"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}